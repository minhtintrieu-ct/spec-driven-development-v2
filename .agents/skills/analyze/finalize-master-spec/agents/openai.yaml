interface:
  display_name: "Finalize Master Spec"
  short_description: "Optionally create the sprint PR"
  default_prompt: "Use $finalize-master-spec to ask whether a finished package should create a PR into spec/<YY.WW>, then follow the fixed happy path: git CLI for branch operations, gh CLI for PR creation and reviewer requests, and Atlassian MCP only for the final Jira handoff comment."
