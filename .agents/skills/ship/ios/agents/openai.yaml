interface:
  display_name: "Ship iOS"
  short_description: "Prepare iOS implementation from the platform spec package"
  default_prompt: |
    Use $ship-ios when `ship` is invoked with `platform = ios`.
    Inherit the shared ship base first, then prepare the current iOS working repo for implementation with the machine helper.
