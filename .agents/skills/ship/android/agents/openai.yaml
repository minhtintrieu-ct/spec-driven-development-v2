interface:
  display_name: "Ship Android"
  short_description: "Prepare Android implementation from the platform spec package"
  default_prompt: |
    Use $ship-android when `ship` is invoked with `platform = android`.
    Inherit the shared ship base first, then prepare the current Android working repo for implementation with the machine helper.
