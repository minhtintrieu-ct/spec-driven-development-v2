interface:
  display_name: "Ship QE"
  short_description: "Prepare automation implementation from the platform spec package"
  default_prompt: |
    Use $ship-qe when `ship` is invoked with `platform = qe`.
    Inherit the shared ship base first, then prepare the current automation working repo for implementation with the machine helper.
